/*
 * Copyright Codeplay Software Ltd.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use these files except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// DO NOT MODIFY BY HAND
// This file was automatically generated by generate_gather_tests.py.
// Results calculated using Tensorflow v2.11.0.

#include <gtest/gtest.h>

#include <vector>

#include "test/gather/gather_fixture.h"
#include "test/types/cartesian_product.h"
#include "test/types/kernel_data_types.h"
#include "test/types/test_backend_types.h"
#include "test/types/to_gtest_types.h"

using DataTypeList = sycldnn::types::KernelDataTypes;
using Backends = sycldnn::types::DefaultBackendTypes;

using TypeBackendPairs =
    sycldnn::types::CartesianProduct<DataTypeList, Backends>::type;

using namespace sycldnn;
using GTestTypePairs = sycldnn::types::ToGTestTypes<TypeBackendPairs>::type;

using IndexDataType = int32_t;  // or int64_t

template <typename Pair>
using Gather1D = GatherFixture<Pair, IndexDataType>;
TYPED_TEST_SUITE(Gather1D, GTestTypePairs);
TYPED_TEST(Gather1D, G1D_Axis_Neg1_Inp5_Ind1) {
  using DataType = typename TestFixture::DataType;
  const std::vector<DataType> exp_out = {3.};
  const std::vector<IndexDataType> indices = {2};
  gather::GatherParams params;
  params.axis = -1;
  params.indices_dims = {1};
  params.input_dims = {5};
  this->test_gather(exp_out, params, indices);
}
TYPED_TEST(Gather1D, G1D_Axis_Neg1_Inp5_Ind5) {
  using DataType = typename TestFixture::DataType;
  const std::vector<DataType> exp_out = {5., 3., 2., 4., 3.};
  const std::vector<IndexDataType> indices = {4, 2, 1, 3, 2};
  gather::GatherParams params;
  params.axis = -1;
  params.indices_dims = {5};
  params.input_dims = {5};
  this->test_gather(exp_out, params, indices);
}
TYPED_TEST(Gather1D, G1D_Axis_Neg1_Inp5_Ind5x2) {
  using DataType = typename TestFixture::DataType;
  const std::vector<DataType> exp_out = {4., 2., 2., 1., 2.,
                                         2., 1., 1., 2., 4.};
  const std::vector<IndexDataType> indices = {3, 1, 1, 0, 1, 1, 0, 0, 1, 3};
  gather::GatherParams params;
  params.axis = -1;
  params.indices_dims = {5, 2};
  params.input_dims = {5};
  this->test_gather(exp_out, params, indices);
}
TYPED_TEST(Gather1D, G1D_Axis_0_Inp5_Ind1) {
  using DataType = typename TestFixture::DataType;
  const std::vector<DataType> exp_out = {5.};
  const std::vector<IndexDataType> indices = {4};
  gather::GatherParams params;
  params.axis = 0;
  params.indices_dims = {1};
  params.input_dims = {5};
  this->test_gather(exp_out, params, indices);
}
TYPED_TEST(Gather1D, G1D_Axis_0_Inp5_Ind5) {
  using DataType = typename TestFixture::DataType;
  const std::vector<DataType> exp_out = {1., 1., 5., 2., 4.};
  const std::vector<IndexDataType> indices = {0, 0, 4, 1, 3};
  gather::GatherParams params;
  params.axis = 0;
  params.indices_dims = {5};
  params.input_dims = {5};
  this->test_gather(exp_out, params, indices);
}
TYPED_TEST(Gather1D, G1D_Axis_0_Inp5_Ind5x2) {
  using DataType = typename TestFixture::DataType;
  const std::vector<DataType> exp_out = {3., 5., 3., 5., 1.,
                                         1., 2., 4., 5., 5.};
  const std::vector<IndexDataType> indices = {2, 4, 2, 4, 0, 0, 1, 3, 4, 4};
  gather::GatherParams params;
  params.axis = 0;
  params.indices_dims = {5, 2};
  params.input_dims = {5};
  this->test_gather(exp_out, params, indices);
}