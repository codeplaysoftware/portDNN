/*
 * Copyright Codeplay Software Ltd.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use these files except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// DO NOT MODIFY BY HAND
// This file was automatically generated by generate_reduce_tests.py.
// Results calculated using Tensorflow v2.8.0.

#include <gtest/gtest.h>
#include <vector>

#include "portdnn/reduce/operators.h"
#include "test/reduce/reduce_event_dependencies_fixture.h"
#include "test/types/cartesian_product.h"
#include "test/types/kernel_data_types.h"
#include "test/types/test_backend_types.h"
#include "test/types/to_gtest_types.h"

using DataTypeList = sycldnn::types::KernelDataTypes;

using GTestTypeList = sycldnn::types::ToGTestTypes<DataTypeList>::type;

template <typename T>
using ReduceAdd = ReduceEventFixture<T, sycldnn::reduce::Add>;
TYPED_TEST_SUITE(ReduceAdd, GTestTypeList);
TYPED_TEST(ReduceAdd, Batch1Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceAdd, Batch1Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceAdd, Batch1Outer11Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceAdd, Batch1Outer11Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceAdd, Batch2Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceAdd, Batch2Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceAdd, Batch2Outer11Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 11;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceAdd, Batch2Outer11Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 11;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}

template <typename T>
using ReduceMean = ReduceEventFixture<T, sycldnn::reduce::Mean>;
TYPED_TEST_SUITE(ReduceMean, GTestTypeList);
TYPED_TEST(ReduceMean, Batch1Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMean, Batch1Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMean, Batch1Outer11Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMean, Batch1Outer11Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMean, Batch2Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMean, Batch2Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMean, Batch2Outer11Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 11;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMean, Batch2Outer11Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 11;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}

template <typename T>
using ReduceMax = ReduceEventFixture<T, sycldnn::reduce::Max>;
TYPED_TEST_SUITE(ReduceMax, GTestTypeList);
TYPED_TEST(ReduceMax, Batch1Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMax, Batch1Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMax, Batch1Outer11Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMax, Batch1Outer11Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMax, Batch2Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMax, Batch2Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}

template <typename T>
using ReduceMin = ReduceEventFixture<T, sycldnn::reduce::Min>;
TYPED_TEST_SUITE(ReduceMin, GTestTypeList);
TYPED_TEST(ReduceMin, Batch1Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMin, Batch1Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMin, Batch1Outer11Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMin, Batch1Outer11Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 1;
  const int outer = 11;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMin, Batch2Outer1Inner1) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 1;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
TYPED_TEST(ReduceMin, Batch2Outer1Inner11) {
  using DataType = typename TestFixture::DataType;
  const int batches = 2;
  const int outer = 1;
  const int inner = 11;
  const DataType max_input_val = 2048.0;
  this->run(batches, outer, inner, max_input_val);
}
